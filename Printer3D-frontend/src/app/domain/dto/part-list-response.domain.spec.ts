import { PartListResponse } from './part-list-response.domain';

describe('PartListResponse', () => {
  it('should create an instance', () => {
    expect(new PartListResponse()).toBeTruthy();
  });
});
