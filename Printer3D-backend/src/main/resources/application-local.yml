server:
  port: ${PORT}
  version: 0.4.5
info:
  tags:
    environment: production
    version: '@project.version@'

spring:
  jpa:
#    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  boot:
    admin:
      client:
        url: http://localhost:8000
        username: springmanager
        password: 01.One.2020
        instance:
          service-base-url: http://localhost:${PORT}
  datasource:
    url: ${JDBC_DATABASE_URL}
    driver-class-name: org.postgresql.Driver
    platform: postgres
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  liquibase:
    liquibase-schema: public
    change-log: classpath:/db/changelog/db.changelog-master.yaml

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
